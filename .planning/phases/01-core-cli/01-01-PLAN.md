---
phase: 01-core-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - todo.js
  - package.json
autonomous: true
requirements:
  - CMD-01
  - STOR-01
  - STOR-02

estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Running `node todo.js add "buy milk"` prints `Added [1]: buy milk` to stdout
    - Running `node todo.js add "buy milk"` creates `todos.json` in the current directory (STOR-02)
    - Running `node todo.js add "write tests"` after the first add prints `Added [2]: write tests` and the file contains both items (STOR-01)
    - Running `node todo.js` with an unknown command prints an error to stderr and exits with code 1
    - After `npm link`, running `todo add "buy milk"` from any directory produces the same behavior as `node todo.js add "buy milk"` (D-01)
    - "[ASSUMPTION] CMD-01 empty text: `todo add ""` or `todo add` with no text is treated as an add with an empty string text — no special validation required in Phase 1 (unresolved edge: empty input)"
    - "[ASSUMPTION] STOR-01 encoding: todos.json is written as UTF-8 (Node.js fs.writeFileSync default) — no explicit encoding decision was made"
    - "[ASSUMPTION] STOR-01 concurrency: concurrent writes to todos.json are not guaranteed safe — single-user CLI tool, no file locking implemented in Phase 1"
    - "[ASSUMPTION] STOR-01 idempotency: running `todo add` twice with the same text creates two separate items with different IDs — no deduplication"
    - "[ASSUMPTION] STOR-02 ordering: todos.json array is ordered by insertion order (append-only); no sort step"
    - "[ASSUMPTION] CMD-01 adjacency: adding items back-to-back produces sequential IDs (no gaps unless items were previously deleted, which is not in scope)"
  artifacts:
    - todo.js — entry point with shebang, command dispatch, cmdAdd, loadTodos, saveTodos
    - package.json — with name, version, and bin.todo pointing to ./todo.js
  key_links:
    - package.json bin.todo → todo.js (must be ./todo.js — npm link reads this path)
    - TODOS_FILE = path.join(process.cwd(), 'todos.json') — per-directory storage (D-05, STOR-01)
    - saveTodos writes UTF-8 JSON with 2-space indent; loadTodos returns [] when file absent (STOR-02 auto-create behavior)
  prohibitions: []
  assumption_flags:
    - "CMD-01/empty: empty text string treated as valid input — no Phase 1 guard"
    - "STOR-01/concurrency: no file locking — single-user assumption"
    - "STOR-01/idempotency: duplicate text creates duplicate item"
    - "STOR-02/unclassified: no special handling if directory is read-only — will throw Node.js ENOENT/EACCES naturally"
---

**As a** developer,
**I want to** run `todo add "buy milk"` from any directory
**so that** the task is saved to a local `todos.json` and I see its ID immediately.

<objective>
Scaffold the Walking Skeleton: create `package.json` with the `bin` entry and `todo.js` with shebang, command dispatch switch, and a working `cmdAdd` function backed by real file I/O (`loadTodos` / `saveTodos`). After this plan, `npm link` registers the `todo` binary and `todo add "text"` works end-to-end.

Purpose: Proves the full stack — file I/O, command routing, stdout, and npm link — before adding the remaining commands.
Output: `todo.js`, `package.json`
</objective>

<execution_context>
@C:/Users/janbf/GSDToDo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/janbf/GSDToDo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-cli/01-CONTEXT.md
@.planning/phases/01-core-cli/01-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "todo add" — scaffold todo.js + package.json wired to file I/O</name>
  <files>todo.js, package.json</files>
  <read_first>
    .planning/phases/01-core-cli/01-PATTERNS.md — all pattern excerpts (shebang, dispatch switch, loadTodos, saveTodos, cmdAdd, package.json shape, TODOS_FILE constant)
    .planning/phases/01-core-cli/01-CONTEXT.md — decisions D-01 through D-07
  </read_first>
  <action>
    Create package.json at the project root with: name "gsdtodo", version "1.0.0", bin.todo pointing to "./todo.js". No other fields required (D-01, per PATTERNS.md package.json shape).

    Create todo.js at the project root. The file MUST begin with the shebang line `#!/usr/bin/env node` on line 1 with no preceding blank line (D-02). Require only `fs` and `path` from Node.js built-ins (D-07).

    Define the constant TODOS_FILE as `path.join(process.cwd(), 'todos.json')` (D-05, STOR-01 — per-directory storage, never a global path).

    Implement loadTodos(): checks whether TODOS_FILE exists using `fs.existsSync`; returns empty array `[]` if absent (this is the STOR-02 auto-create behavior — no error thrown); otherwise reads the file as UTF-8 and parses it with `JSON.parse`.

    Implement saveTodos(todos): writes the todos array to TODOS_FILE using `fs.writeFileSync` with `JSON.stringify(todos, null, 2)` as the content (2-space indent, UTF-8 default).

    Implement cmdAdd(args): joins all args into a single string as the text. Calls loadTodos(). Assigns the next ID as `todos.length > 0 ? Math.max(...todos.map(t => t.id)) + 1 : 1` (monotonic, survives deletions). Pushes `{ id, text, done: false }` onto the array. Calls saveTodos(). Prints `Added [${id}]: ${text}` to stdout (D-03 — exact format: bracket-wrapped ID, colon separator, then text).

    Add the command dispatch block using `const [,, command, ...args] = process.argv`. Implement a switch statement with cases 'add' calling `cmdAdd(args)`, and a default branch that prints `Unknown command: ${command}` to stderr using `console.error` and calls `process.exit(1)` (per PATTERNS.md error exit pattern).

    Do NOT use fenced code blocks in the action; implement per the patterns above. Do NOT require any npm packages — only `fs` and `path` (D-07). Do NOT use a global file path like `path.join(os.homedir(), ...)` — always `process.cwd()` (D-05).
  </action>
  <verify>
    From the project root, run:
      node todo.js add "buy milk"
    Expected stdout: `Added [1]: buy milk`
    Expected: todos.json created in current directory containing an array with one item: `[{"id":1,"text":"buy milk","done":false}]`

    Then run:
      node todo.js add "write tests"
    Expected stdout: `Added [2]: write tests`
    Expected: todos.json now contains two items with IDs 1 and 2.

    Then verify npm link registration:
      npm link
      todo add "linked test"
    Expected stdout: `Added [3]: linked test`
    Expected: todos.json in the project root now has three items.
  </verify>
  <done>
    `node todo.js add "buy milk"` prints `Added [1]: buy milk`; todos.json is created in the current directory with the correct JSON schema `[{"id":1,"text":"buy milk","done":false}]`; a second add produces ID 2; `npm link` succeeds and `todo add` works from the terminal.
  </done>
  <acceptance_criteria>
    - `todo.js` line 1 is exactly `#!/usr/bin/env node` (no preceding blank line)
    - `package.json` contains `"bin": { "todo": "./todo.js" }`
    - `node todo.js add "buy milk"` exits with code 0 and prints exactly `Added [1]: buy milk` to stdout
    - After the add, `todos.json` exists in the working directory and is valid JSON containing `[{"id":1,"text":"buy milk","done":false}]`
    - `node todo.js add "write tests"` (second call) prints `Added [2]: write tests` and todos.json has exactly 2 items
    - `node todo.js unknowncmd` prints to stderr and exits with code 1
    - `npm link` completes without error and `todo add "x"` resolves to the project's todo.js
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → cmdAdd | User-controlled text enters the system via process.argv; stored as-is in todos.json |
| todo.js → filesystem | Writes todos.json to process.cwd(); path is derived from Node.js built-in, not from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | todos.json write | low | accept | Single-user local CLI; no multi-user trust boundary; file is human-readable and intentionally editable |
| T-01-02 | Information Disclosure | error stderr output | low | accept | Errors print user-supplied command names; no internal paths or secrets exposed |
| T-01-03 | Denial of Service | fs.writeFileSync crash on read-only dir | low | accept | Phase 1 tool; Node.js throws EACCES naturally; user is the operator |
| T-01-SC | Tampering | npm installs | low | accept | Zero npm installs in this phase — Node.js built-ins only (D-07); no supply-chain surface |
</threat_model>

<verification>
End-to-end check after plan completion:

1. `node todo.js add "buy milk"` → stdout is `Added [1]: buy milk`, exit 0
2. `cat todos.json` → valid JSON array with one item, schema `{id, text, done}`
3. `node todo.js add "write tests"` → stdout is `Added [2]: write tests`, todos.json has 2 items
4. `node todo.js badcmd` → stderr contains error message, exit code 1
5. `npm link && todo add "linked"` → stdout is `Added [3]: linked`, exit 0
</verification>

<success_criteria>
The `todo add` command works end-to-end: adds items with sequential IDs, persists them to todos.json in the current directory, prints `Added [id]: text` confirmation, and is accessible as `todo` after `npm link`. Unknown commands exit with code 1.
</success_criteria>

## Artifacts this phase produces

| Symbol | Type | File | Notes |
|--------|------|------|-------|
| `#!/usr/bin/env node` | shebang | todo.js line 1 | Required for npm link to execute the file |
| `TODOS_FILE` | constant | todo.js | `path.join(process.cwd(), 'todos.json')` |
| `loadTodos()` | function | todo.js | Returns `[]` when file absent; parses JSON otherwise |
| `saveTodos(todos)` | function | todo.js | Writes `JSON.stringify(todos, null, 2)` to TODOS_FILE |
| `cmdAdd(args)` | function | todo.js | Appends item, saves, prints `Added [id]: text` |
| command dispatch switch | block | todo.js | `const [,, command, ...args] = process.argv` + switch |
| `bin.todo` | package.json field | package.json | Points to `./todo.js`; required for npm link |
| `todos.json` | runtime artifact | `<cwd>/todos.json` | Created on first add; array of `{id, text, done}` |

<output>
Create `.planning/phases/01-core-cli/01-01-SUMMARY.md` when done.
</output>
