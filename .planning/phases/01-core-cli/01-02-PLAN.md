---
phase: 01-core-cli
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - todo.js
autonomous: true
requirements:
  - CMD-02
  - CMD-03
  - CMD-04

estimate:
  tokens: 32000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Running `todo list` after adding two items prints both items in insertion order, each formatted as `[id]: text`
    - Running `todo list` after marking item 1 done renders item 1 with Unicode strikethrough on both the bracket-ID portion and the text (D-06 — no ANSI escape codes)
    - Running `todo done 1` after adding an item prints `Done: buy milk` to stdout (D-04 — exact format)
    - Running `todo done 1` persists `done: true` on the item so subsequent `todo list` shows it with strikethrough
    - "[ASSUMPTION] CMD-02 ordering: items are listed in array (insertion) order — no sort step; the spec does not define output order, so insertion order is the stable default"
    - "[ASSUMPTION] CMD-02 empty list: when todos.json is absent or empty, `todo list` prints nothing and exits 0 — the friendly no-items message is Phase 2 (UX-02); no special empty-state output in this phase"
    - "[ASSUMPTION] CMD-03 encoding: strikethrough is applied per character using Unicode U+0336 (combining long stroke overlay); multi-codepoint grapheme clusters (emoji, accented chars) will have the overlay on each code point — no grapheme segmentation in Phase 1"
    - "[ASSUMPTION] CMD-03 adjacency: strikethrough is applied to the full formatted line `[id]: text`, not just the text portion — entire label string is struck through when done"
    - "[ASSUMPTION] CMD-04 adjacency: if an item is already done and `todo done <id>` is called again, it sets done=true again and prints `Done: text` — no guard against re-doing a done item in Phase 1"
    - "[ASSUMPTION] CMD-04 empty: `todo done` with no ID argument or a non-numeric argument — parseInt returns NaN; NaN !== any id; prints error and exits 1 (natural behavior of the pattern from PATTERNS.md)"
  artifacts:
    - todo.js — expanded with cmdList (strikethrough helper) and cmdDone functions; list and done cases added to dispatch switch
  key_links:
    - strikethrough(str) helper uses `str.split('').map(c => c + '̶').join('')` — Unicode U+0336 per character (D-06)
    - cmdList reads todos via loadTodos() and formats each as `[id]: text` or struck-through variant
    - cmdDone finds item by integer id, sets done=true, saves, prints `Done: text` (D-04)
    - dispatch switch gains 'list' and 'done' cases alongside existing 'add' case
  prohibitions: []
  assumption_flags:
    - "CMD-02/ordering: insertion order assumed stable"
    - "CMD-02/empty: no empty-state output in Phase 1 (UX-02 is Phase 2)"
    - "CMD-03/encoding: per-codepoint strikethrough — no grapheme segmentation"
    - "CMD-04/adjacency: re-done item prints confirmation again — no guard"
    - "CMD-04/empty: NaN id finds no item, exits 1 naturally"
---

**As a** developer,
**I want to** run `todo list` and `todo done <id>`
**so that** I can see all my tasks (with strikethrough for completed ones) and mark them done.

<objective>
Expand todo.js with two remaining commands: `cmdList` (renders all items, applying Unicode strikethrough to completed items per D-06) and `cmdDone` (marks an item done by integer ID and prints confirmation per D-04). Add 'list' and 'done' cases to the existing dispatch switch. After this plan, all three commands are working and the phase is complete.

Purpose: Delivers the remaining two user-visible capabilities on top of the proven storage foundation from Plan 01.
Output: `todo.js` (modified — two new functions + two new switch cases)
</objective>

<execution_context>
@C:/Users/janbf/GSDToDo/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/janbf/GSDToDo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-cli/01-CONTEXT.md
@.planning/phases/01-core-cli/01-PATTERNS.md
@.planning/phases/01-core-cli/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strikethrough helper and cmdList to todo.js</name>
  <files>todo.js</files>
  <read_first>
    todo.js — read current file state before editing (see existing dispatch switch, function positions)
    .planning/phases/01-core-cli/01-PATTERNS.md — strikethrough pattern and cmdList pattern sections
  </read_first>
  <action>
    Read todo.js first to see its current state.

    Add a `strikethrough(str)` function that maps each character to itself followed by the Unicode combining long stroke overlay character U+0336 (the literal character, not an escape sequence in comments). The implementation is: split the string into individual characters, append U+0336 to each, join and return. This must NOT use ANSI escape codes, ANSI color libraries, or any npm package (D-06, D-07).

    Add a `cmdList()` function that calls `loadTodos()`. For each item in the returned array, formats the label as `[${t.id}]: ${t.text}`. If the item's `done` field is true, wrap the entire label string in `strikethrough()`. Print each (possibly struck-through) label to stdout using `console.log`.

    In the dispatch switch, add a `case 'list':` that calls `cmdList()` and a `return` or `break`. Place the new case alongside the existing 'add' case, before the `default` branch.

    Do NOT modify loadTodos, saveTodos, cmdAdd, the TODOS_FILE constant, or the shebang line. Do NOT add any new `require()` calls — only `fs` and `path` are permitted (D-07).
  </action>
  <verify>
    From the project root, with todos.json already containing items from Plan 01, run:
      todo list
    Expected: each item printed on its own line as `[1]: buy milk`, `[2]: write tests`, etc. (insertion order)

    Then run:
      todo done 1
      todo list
    Expected: item 1 renders with all characters struck through (visible Unicode combining characters on each character of `[1]: buy milk`); item 2 renders normally.
  </verify>
  <done>
    `todo list` prints all items in insertion order formatted as `[id]: text`; completed items render with Unicode strikethrough (U+0336 per character) applied to the full label; no ANSI codes are present.
  </done>
  <acceptance_criteria>
    - todo.js contains a function named `strikethrough` that appends U+0336 to each character of its string argument
    - `todo list` with two items in todos.json prints exactly two lines: `[1]: buy milk` and `[2]: write tests` (no extra whitespace, no numbering prefix)
    - After `todo done 1`, `todo list` output for item 1 contains the Unicode combining stroke character (U+0336) — verifiable with: `todo list | cat -v | head -1` showing the combining character bytes, or `node -e "const s = require('child_process').execSync('todo list').toString().split('\n')[0]; console.log([...s].some(c => c === '̶'))"` returning true
    - `todo list` output does NOT contain ESC (0x1B) bytes — verifiable with: `todo list | xxd | grep -c '1b '` returning 0
    - The dispatch switch in todo.js has a `case 'list':` branch
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add cmdDone to todo.js</name>
  <files>todo.js</files>
  <read_first>
    todo.js — read current file state after Task 1 to see updated switch and existing functions
    .planning/phases/01-core-cli/01-PATTERNS.md — cmdDone pattern and error exit pattern sections
  </read_first>
  <action>
    Read todo.js first to see its current state after Task 1.

    Add a `cmdDone(args)` function. Parse the first element of args as an integer using `parseInt(args[0], 10)`. Call `loadTodos()`. Find the item where `t.id === id` using `Array.prototype.find`. If no item is found (including when id is NaN), print `No todo with id ${id}` to stderr using `console.error` and call `process.exit(1)`. If the item is found, set `item.done = true`, call `saveTodos(todos)`, and print `Done: ${item.text}` to stdout (D-04 — exact format: "Done: " prefix followed by the raw item text).

    In the dispatch switch, add a `case 'done':` that calls `cmdDone(args)`. Place it alongside the 'add' and 'list' cases, before the `default` branch.

    Do NOT modify strikethrough, cmdList, loadTodos, saveTodos, cmdAdd, the TODOS_FILE constant, or the shebang line. Do NOT add any new `require()` calls (D-07).
  </action>
  <verify>
    Run the full end-to-end acceptance sequence:
      node todo.js add "buy milk"
      node todo.js add "write tests"
      node todo.js list
      node todo.js done 1
      node todo.js list

    Expected sequence:
      Added [1]: buy milk
      Added [2]: write tests
      [1]: buy milk
      [2]: write tests
      Done: buy milk
      [struck-through [1]: buy milk]
      [2]: write tests

    Then verify the error path:
      node todo.js done 999
    Expected: prints to stderr, exits with code 1.
  </verify>
  <done>
    `todo done 1` sets `done: true` on the matching item and prints `Done: buy milk`; subsequent `todo list` shows item 1 with strikethrough and item 2 normally; `todo done 999` exits with code 1 and prints an error.
  </done>
  <acceptance_criteria>
    - `todo done 1` (with item 1 in todos.json) prints exactly `Done: buy milk` to stdout and exits 0
    - After `todo done 1`, todos.json item with id 1 has `"done": true`
    - `todo done 999` prints to stderr (not stdout) and exits with code 1
    - `todo done` (no argument, NaN id) exits with code 1 and prints an error message
    - The dispatch switch in todo.js has a `case 'done':` branch
    - The full three-command sequence (add → list → done → list) matches the expected output described in the verify block
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → cmdDone | User-supplied ID string parsed as integer via parseInt; no eval or dynamic execution |
| todos.json → stdout | Stored task text is printed to stdout as-is; no HTML rendering surface, no shell execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | cmdList stdout | low | accept | Task text is printed as-is; no secrets or internal paths in output; user supplied all text |
| T-02-02 | Tampering | cmdDone id parse | low | accept | parseInt(arg, 10) produces NaN for non-numeric input; NaN !== any stored id; safe natural handling |
| T-02-03 | Elevation of Privilege | strikethrough output | low | accept | Unicode combining characters are purely cosmetic display; no control character injection — ANSI codes are explicitly forbidden (D-06) |
  T-02-SC | Tampering | npm installs | low | accept | No new packages installed in this plan (D-07); no supply-chain surface |
</threat_model>

<verification>
Full phase end-to-end check after both plans complete:

1. `todo add "buy milk"` → `Added [1]: buy milk`, exit 0
2. `todo add "write tests"` → `Added [2]: write tests`, exit 0
3. `todo list` → two lines: `[1]: buy milk` and `[2]: write tests`
4. `todo done 1` → `Done: buy milk`, exit 0
5. `todo list` → item 1 with Unicode strikethrough, item 2 normal
6. `todo done 999` → error to stderr, exit 1
7. `cat todos.json` → valid JSON array, item 1 has `"done": true`, item 2 has `"done": false`
8. New terminal session: `cd /tmp && todo list` → shows items from the test directory's todos.json — WAIT, this actually tests per-directory behavior: running from /tmp shows no items (different cwd), confirming STOR-01 per-directory isolation
</verification>

<success_criteria>
All three commands work end-to-end: `todo add` creates and saves items; `todo list` renders all items with Unicode strikethrough on completed ones; `todo done <id>` marks items complete. Items persist across terminal sessions. Phase 1 requirements CMD-01, CMD-02, CMD-03, CMD-04, STOR-01, STOR-02 are all satisfied.
</success_criteria>

## Artifacts this phase produces

| Symbol | Type | File | Notes |
|--------|------|------|-------|
| `strikethrough(str)` | function | todo.js | Applies U+0336 per character; no ANSI |
| `cmdList()` | function | todo.js | Reads todos, renders each with optional strikethrough |
| `cmdDone(args)` | function | todo.js | Marks item done by integer ID, prints `Done: text` |
| `case 'list':` | switch branch | todo.js | Routes `todo list` to cmdList |
| `case 'done':` | switch branch | todo.js | Routes `todo done <id>` to cmdDone |

<output>
Create `.planning/phases/01-core-cli/01-02-SUMMARY.md` when done.
</output>
